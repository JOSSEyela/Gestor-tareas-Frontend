import type { TaskFilters } from "../types";

interface FilterBarProps {
  filters: TaskFilters;
  onChange: (filters: TaskFilters) => void;
}


export function FilterBar(_props: FilterBarProps) {
  return null;
}
